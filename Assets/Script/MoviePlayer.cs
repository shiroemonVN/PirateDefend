using UnityEngine;
using System.Collections;

public class MoviePlayer : MonoBehaviour {
    public static bool played; 
	// Use this for initialization
	void Start () {
        //Handheld.PlayFullScreenMovie("video.mp4", Color.black, FullScreenMovieControlMode.CancelOnInput,FullScreenMovieScalingMode.Fill);
	}
	
	// Update is called once per frame
	void Update () {

	}
}
