using UnityEngine;
using System.Collections;

public class RotatePlayerShip : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //gameObject.transform.rotation = new Quaternion(270f, gameObject.transform.rotation.y, gameObject.transform.rotation.z, gameObject.transform.rotation.w);
	}
	
	// Update is called once per frame
	void Update () {
        //transform.Rotate(Vector3.right,270f);
	}
}
